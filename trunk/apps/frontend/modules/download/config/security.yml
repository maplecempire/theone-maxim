downloadGuide:
  is_secure:   on
  credentials: [[ MAXIM_DISTRIBUTOR ]]

downloadMt4Pro:
  is_secure:   on
  credentials: [[ MAXIM_DISTRIBUTOR ]]

mt4Pro:
  is_secure:   on
  credentials: [[ MAXIM_DISTRIBUTOR ]]

mt4ProUserGuide:
  is_secure:   on
  credentials: [[ MAXIM_DISTRIBUTOR ]]

all:
  is_secure:  off        # off is the default value anyway